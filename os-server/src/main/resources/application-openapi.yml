spring:
  h2:
    console:
      enabled: true
      path: /h2
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  jpa:
    show-sql: true
  mail:
    host: smtp.gmail.com
    username: open_school_gmail.com
    password: open_school_password
    port: 5000
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true